{
  "name": "cursor-mcp-server",
  "version": "1.0.0",
  "description": "A specialized MCP server for Cursor IDE that manages .cursor/rules directories",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.4"
  },
  "scripts": {
    "inspector": "npx @modelcontextprotocol/inspector node ./cursor_mcp_server.js",
    "generate-config": "node generate-config.js"
  }
}
